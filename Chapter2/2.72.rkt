;; O(n) to find the most frequent symbol in the list
;; O(1) to find the most frequent symbol in the tree
;; It is at the root, so only one car to getit out

;; O(n^2) to find the least frequent symbol
;; n to find the symbol in the list and lg n
;; to walk the tree untill find it. 
